#pragma once

namespace game
{
    // Tag component for entities that should always be rendered, 
    // regardless of visibility logic (e.g. map background).
    struct AlwaysVisibleComponent
    {
    };
}
